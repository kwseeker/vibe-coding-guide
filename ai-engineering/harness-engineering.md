# 驾驭工程

软件工程师逐渐从"代码的编写者"变成"环境的建筑师"。最大的工程挑战是设计让 Agent 可靠工作的控制系统。
当前（202604）并没有什么生产级别开箱即用的 Harness 工程系统。
市面上多是一些多Agent编排系统。
Harness engineering 是一套方法论，也比较难做成一套开箱即用的系统。

参考：
- [驾驭工程](https://www.runoob.com/ai-agent/harness-engineering.html)（菜鸟教程）
- [OpenAI内部的尝试](https://openai.com/zh-Hans-CN/index/harness-engineering/)

TODO：
- 具体都怎么实现？能否在开源项目中找到这些实现案例？

## 工程思想发展

- **提示词工程**
	（定义如何描述清楚一个问题）
	- 澄清任务
	- 约束输出
	- 激发模型已有能力
- **上下文工程**
	（定义上下文的供给机制，确保按需精确供给合适的提示词）
	- 用户输入
	- 历史对话
	- 检索结果
	- 工具返回
	- 当前任务状态
	- 中间产物
	- 系统规则
	- 安全约束
- **驾驭工程**
	（约束模型按正确的方向执行且可以自行纠偏）
	（LangChain 工程师将 Agent 除去模型剩余的部分都算进 Harness）
	-  **信息边界**
		- 角色和目标定义
		- 信息选择和裁剪
			（上下文不是越多越好，而是越相关越好）
		- 结构化组织
			（将提示词（比如固定规则、任务列表、运行状态、外部数据）按分层结构存放并交代清楚，方便LLM按需检索）
			（结构化的提示词可以防止模型漏重点、忘记约束和自我污染。）
	- **工具系统**
		（接触真实世界的窗口）
		- 合理提供工具
			（研究人员发现工具给的太少能力不够，工具给的太多模型又会乱用）
		- 规定什么时候该调什么工具
		- 结果怎么重新喂回
			工具获取的结果不应该直接返回，需要根据任务目标需要做提炼筛选，保持与任务的相关性。
	- **执行编排**
		- 理解目标
		- 判断信息是否完整 -> 不够就补
		- 继续分析 -> 生成输出
		- 检查输出 -> 不满足就修正重试
	- **状态与记忆**
		- 当前任务状态
		- 会话中间结果
		- 长期记忆与偏好
	- **评估和观测**
		（评估生成结果的质量）
		- 输出验收
		- 环境验证
		- 自动测试
		- 日志和指标
		- 错误归因
	- **约束、校验与失败恢复**
		- 约束
		- 校验
		- 恢复

## 四大支柱

- **上下文工程**
- **架构约束**
- **反馈循环**
- **垃圾回收**

## 自我改进闭环
（具体怎么做？）

- Agent 遇困难
- 识别缺失能力
- 编写修复代码
- Harness 改进

## 实现架构

### 三Agent架构

**架构组成**：

- **Planner** 扩展需求
- **Generator** 生成代码
- **Evaluator** 交互验证
	比如使用 Playwright 工具做交互式验证和打分。
	解决自评估的系统缺陷，Agent总是对自己生成的内容表现地很自信。

**三 Agent 架构产品案例**：

- **Aletheia**
- **ADK** （开源 Agent 开发套件）

### 多 Agent Coding Harness 架构图

```
┌──────────────────────────────────────────────┐
│                🧠 控制层 (Control Plane)       │
├──────────────────────────────────────────────┤
│                                              │
│  🎯 Task Planner / Goal Manager              │
│  - 拆解任务 (PRD → 子任务)                    │
│  - 动态调整计划                              │
│                                              │
│  🧑‍💼 Manager Agent（调度核心）              │
│  - 分配任务给各子 Agent                      │
│  - 跟踪进度 (progress log)                   │
│  - 决策：继续 / 重试 / 回滚                   │
│                                              │
│  🔁 Execution Loop Engine                    │
│  - plan → act → observe → reflect            │
│  - 长时运行（关键能力）                      │
│                                              │
│  🧪 Evaluation / Critic System               │
│  - 自动评估代码质量                          │
│  - 单测 / Lint / 静态分析                    │
│  - Self-healing（失败自动重试）              │
│                                              │
└──────────────────────────────────────────────┘

                ↓ 任务分发 / 状态同步

┌──────────────────────────────────────────────┐
│              ⚙️ 执行层 (Agent Layer)          │
├──────────────────────────────────────────────┤
│                                              │
│  👨‍💻 Coding Agent                          │
│  - 写代码 / 修改代码                         │
│  - 调用工具（IDE / shell / git）             │
│                                              │
│  🔍 Research Agent                          │
│  - 查文档 / API / StackOverflow              │
│  - 构建上下文                                │
│                                              │
│  🧪 Testing Agent                           │
│  - 生成测试用例                              │
│  - 执行测试                                  │
│                                              │
│  🧹 Refactor Agent                          │
│  - 重构代码                                  │
│  - 优化结构                                  │
│                                              │
│  🧠 Memory Agent（可选）                     │
│  - 总结经验                                  │
│  - 写入长期记忆                              │
│                                              │
└──────────────────────────────────────────────┘

                ↓ 工具调用 / 数据读写

┌──────────────────────────────────────────────┐
│         🏗️ 基础设施层 (Infra / Harness)       │
├──────────────────────────────────────────────┤
│                                              │
│  🧰 Tool System（工具系统）                  │
│  - File System（读写代码）                   │
│  - Shell / Docker                            │
│  - Browser / Crawler                         │
│  - Git 操作                                  │
│                                              │
│  🧠 Memory System（核心差异点🔥）             │
│  - 短期记忆（上下文窗口）                    │
│  - 长期记忆（Vector DB / 知识库）            │
│  - Artifact（代码、日志、结果）              │
│                                              │
│  🗂️ Context Management                      │
│  - 压缩上下文                                │
│  - 选择性加载                                │
│  - 防止 token 爆炸                           │
│                                              │
│  🔐 Guardrails / 权限控制                    │
│  - 限制危险操作                              │
│  - 审批机制（如删库）                        │
│                                              │
│  🔄 State Store（状态存储）                  │
│  - 任务状态                                  │
│  - agent 状态                                │
│  - 可恢复执行（checkpoint）                  │
│                                              │
│  📊 Observability（可观测性）                │
│  - logs / traces                             │
│  - agent 行为记录                            │
│                                              │
└──────────────────────────────────────────────┘
```

## 功能模块

- **上下文工程**（Context Engineering）
	- **项目指令文件**
		AGENTS.md/ CLAUDE.md: 作为 Agent 启动时读取的知识基座。
	- **动态上下文注入**
		从日志、指标、追踪信息中获取实时信息管道。
	- **上下文隔离**
		子 Agent 作为上下文防火墙，隔离窗口独立运行。
	- **上下文压缩**
		窗口填满时自动丢弃或摘要无关信息，对抗熵增。

- **工具编排**
	- **精选工具集**
		工具不是越多越好，精心策划，移除冗余选项
	- **MCP协议集成**
	- **沙箱隔离**
		执行环境与生产系统隔离，如 Stripe devbox 模式

- **验证机制与硬约束**
	- **确定性约束**（不需要LLM）
		- 自定义 Linter
		- 结构测试
		- Pre-commit
	- **生成-评估分离**
		- 独立评估器
		- Anthropic 模式
	
- **反馈循环**（自我验证循环）
	传统开发中，人类工程师负责代码审查（Code Review）。在驾驭工程中，这个工作变成了"**智能体对智能体**"的方式：Codex 在本地审核自身更改，请求额外审查，循环往复直到通过。
	反馈循环中的钩子可以运行预定义的测试套件，并在失败时带着错误信息循环回到模型，或者提示模型独立评估其代码。如果 AI 写的测试用例"通过"了带有 Bug 的代码，Harness 就会判定测试无效，强迫它重新思考测试边界。

- **状态管理与记忆持续性**

- **可观测性**
	- 执行追踪
	- 质量分级
	- 异常检测
	- 反馈归因
		失败模式追溯到 Harness 缺陷，驱动持续改进。

- **人类接管与生命周期管理**
	- 关键节点暂停让人类确认是否继续

- **熵管理**（Entropy Management）——垃圾回收
	定期清理项目迭代过程中积累的技术债务。
	具体措施：定期运行后台 Codex 任务扫描偏差、更新质量等级、发起针对性重构 PR。此外还有一个专门的 **Doc-gardening Agent**（文档园丁代理），在后台自动扫描文档与代码之间的不一致，发现过时内容就自动提交 PR 修复——Agent 为 Agent 维护文档。
